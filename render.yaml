services:
  - type: web
    name: discord-music-bot
    env: node
    plan: starter
    start: npm start
    envVars:
      - key: DISCORD_TOKEN
        from:
          secret: discord-token
      - key: PORT
        value: "3000"
    autoDeploy: true
    healthCheckPath: /
